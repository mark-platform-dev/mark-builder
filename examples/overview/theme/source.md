# Theme source

The template's built-in default theme, unchanged: cool paper, one deep
spruce accent, system font stacks. The overview is the tool describing
itself, so it wears the default on purpose — a project about a product would
replace this folder with that product's theme.
