node_modules/
.pnpm-store/
dist/
.astro/
**/raw/*
!**/raw/.gitkeep
.DS_Store

# Left behind by `pnpm --dir <pkg>` / `pnpm -C <pkg>`, which treat the package
# as its own workspace. Use `pnpm --filter <pkg>` instead.
template/pnpm-lock.yaml
examples/*/pnpm-lock.yaml
